# Habilita .htaccess (rewrite do SPA e da API) no webroot do container.
<Directory /var/www/html>
    AllowOverride All
    Require all granted
</Directory>

# Encaminha o header Authorization para o PHP (CGI/FPM às vezes o removem).
SetEnvIf Authorization "(.*)" HTTP_AUTHORIZATION=$1
